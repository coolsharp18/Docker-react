sudo: required
services:
    - docker
    
before_install:
    - docker build -t coolsharp18/Docker-react -f Dockerfile.dev . 
    
script:
    - docker run coolsharp18/Docker-react npm run test -- --coverage